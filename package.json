{
    "name": "krushn-calendar",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "frontend",
        "backend"
    ],
    "scripts": {
        "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
        "build": "npm run build --workspace=frontend",
        "postinstall": "npm run build"
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    }
}